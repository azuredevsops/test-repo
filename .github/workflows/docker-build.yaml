name: Build, Scan and Push

on: [push]
jobs: 
  buil-scan-push:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout code
       uses: actions/checkout@v4
     
     - name: Docker Login 
       uses: docker/login-action@v3
       with:
          username: ${{secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
     
     - name: Build Docker Image 
       run: |
         docker build  -t kiranpanchavati/app:v1 .
     
     - name: scan image with trivy
       uses: aquasecurity/trivy-action@0.28.0
       with:
         image-ref: kiranpanchavati/app:v1
         format: table
         exit-code: 1
         ignore-unfixed: true
         severity: CRITICAL,HIGH
     
     - name: Generate SBOM 
       run: |
         trivy image --format cyclonedx --output sbom.json kiranpanchavati/app:v1

     - name: Upload SBOM
       uses: actions/upload-artifact@v4
       with:
         name: sbom-cyclonedx
         path: sbom.json
     
     - name: push docker image
       if: success()
       run: |
        docker push kiranpanchavati/app:v1
